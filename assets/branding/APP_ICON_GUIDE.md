# Primary application icon guide

The primary mark combines a clipboard/document, flowing text, and a typing key/caret. It is used for README and release artwork. Windows executable resources use the related simplified geometric construction so the mark remains legible from 16 through 256 pixels.
